/**
 * 兼容层：历史 import `@lib/image-provider-factory` 转发至 Register 旁 `image-execute`。
 */
export { executeImageGeneration, type ExecuteImageGenerationInput } from '@lib/providers/registers/_shared/image-execute/execute.server'
